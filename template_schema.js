
/**
 * 템플릿 스키마
 *  - 요구사항
 *      + handlebar 와 의 연동할 경우 객체의 스키마 타입 정의
 */

(function(global) {
    'use strict';    

    function template_schema() {

    }

    function schema() {}

    /**
     * 테이블 정보
     *  - 테이블 이름, 갯수 ?
     *  - 컬럼 이름, 갯수
     * param 정보
     * 기본 설정
     * 동작 (기능)
     * 스타일
     * 필수/선택
     * 
     * 추상적 상위 스키마 또는 하위 템플릿
     *  - 공통의 묶음영역
     *  - 재귀적 호출 사용 방법?
     */

    // global.Entity                   = global.Entity || {};
    // global.Entity[entityNamespace]  = global.Entity[entityNamespace] || entity;

}(this));